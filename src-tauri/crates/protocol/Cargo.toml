[package]
name = "opentune-protocol"
version = "0.0.0"
edition = "2021"
license = "GPL-3.0-or-later"
publish = false

[dependencies]
thiserror = "2"
opentune-ini = { path = "../ini" }
opentune-transport = { path = "../transport" }

[dev-dependencies]
opentune-simulator = { path = "../simulator" }
